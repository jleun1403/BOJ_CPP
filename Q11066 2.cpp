#include <bits/stdc++.h>
using namespace std;
int a[501];
int main(){
    int t;
    cin >> t;
    while(t--){
        int n;
        cin >> n;
        memset(a, 0 , sizeof(a));
        for(int i=0; i<n; i++) scanf("%d", &a[i]);
        
    }
}